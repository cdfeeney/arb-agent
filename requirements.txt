httpx>=0.27.0
rapidfuzz>=3.9.0
numpy>=1.26.0
pyyaml>=6.0
rich>=13.7.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
python-dateutil>=2.9.0
twilio>=9.0.0
cryptography>=42.0.0
anthropic>=0.40.0
websockets>=12.0
# Polymarket CLOB order placement (signing, nonce mgmt, L2 API creds).
# Required only when execution.mode=live AND the bot places Polymarket-side
# legs. Lazy-imported in src/exec/polymarket_exchange.py so the bot loads
# without it.
py-clob-client>=0.20.0
